# Changelog técnico — Septiembre 2026

## 2026-09-25

**[Tours/#120]:** vistas guiadas de Finanzas y Horas, secciones nuevas en el tour de Inicio y textos desfasados (ADR-024).
- Auditoría: 14 tours sanos (67 pasos, sin selectores rotos), pero Finanzas y Horas sin tour ni anclas, el sidebar con dos `NavSection` sin `tour=` (el tour de Inicio las saltaba en silencio) y tres pasos que decían menos de lo que hay: `nav-dymmsa` (sin corte ni medidas), `sup-table` (sin días de crédito), `plan-actions` (sin Copiar para Excel).
- `Sidebar.tsx`: `tour` obligatoria en `NavSection` + anclas `nav-finanzas`/`nav-horas`; `dashboard.ts` gana los dos pasos (9); `tours.test.tsx` exige que todo `nav-*` renderizado esté en el tour.
- Cuatro tours nuevos (18 en total): `finance-overview.ts` (6 pasos: `fin-month`, `fin-expenses`, `fin-income`, `fin-closing`, `fin-credit-notes`, `fin-due-weeks` — el overview se envolvió en dos `<section>` para que egresos e ingresos sean bloques), `payables.ts` (3: `pay-new`, `pay-filters`, `pay-table`), `hours.ts` (5: `hrs-week-nav`, `hrs-employee` admin-only, `hrs-week-chart`, `hrs-trend`, `hrs-grid`) y `team.ts` (2: `team-header`, `team-table`). `TourButton` en las cuatro páginas.
- Tests: `tours-finance.test.tsx` (overview con las cards de ingresos y notas presentes; página de facturas con tabla y form a null) y `tours-hours.test.tsx` (5 pasos como admin, 4 como member; Equipo 2 y 1).

- Review del PR #121: `MetricCard` acepta `data-tour` y el cierre se ancla en la propia Card — un `<div>` envolvente se volvía el ítem del grid y la tarjeta dejaba de estirarse a la altura de la fila; los pasos de Finanzas van en orden de pantalla (vencimientos antes que notas de crédito) y un test lo fija con `compareDocumentPosition`; el test del sidebar ya no tiene piso mágico de 8 anclas.

**Motivo:** las vistas guiadas se hicieron en el #74 cuando Finanzas y Horas no existían; ocho módulos después el tour de Inicio ni las mencionaba. Reunión 2026-09-24.

## 2026-09-24

**[Docs/#118]:** documentación in-app al día + sección del Asistente generada del manifiesto (ADR-031).
- Correcciones de lo que era falso desde julio: el Excel URREA se descarga desde Planificar compra y su criterio es pertenencia al catálogo (no `brand='URREA'`); al confirmar recepción solo el excedente entra al inventario (ADR-019); el detalle de orden lista Planificar compra/corte, Formato de Entrega e Imprimir; la tabla de acciones y el flujo del sistema (ahora 10 pasos, con planificar compra y corte).
- Secciones nuevas en `src/app/dashboard/docs/sections/`: Planificar compra, Corte y medidas de material, Proveedores, Finanzas, Horas y Equipo, Tablas/vistas guiadas/atajos, Asistente (IA). `DocSection`/`Note`/`Sub`/`List` como marco común; `page.tsx` solo suma imports e índice.
- `src/lib/mcp/manifest.ts` (44 entradas) + `tests/mcp/manifest.test.ts` (igualdad en ambos sentidos con `server.ts`, cinco escrituras exactas con límites, Odoo sin escrituras). `AssistantSection` se pinta del manifiesto; `tests/components/DocsSections.test.tsx` fija que cada capacidad aparece con su ejemplo y que las cinco acciones llevan su límite.
- La doc no menciona la bitácora de facturas (ADR-028: un member la lee).
- Review del PR #119: la regla "Revisar" del planificador estaba explicada al revés en dos lugares (dispara cuando el **excedente** parado es ≥ 80 %, es decir con un resto chico — `parkedPct = excess / std`), corregida; el bullet de "quién marcó pagada" se quitó de `FinanceSection` para cumplir lo que el ADR-031 afirma; el test del manifiesto exige que el conteo por regex coincida con los `registerTool(` del archivo; "sin marca" acotado a Inventario; `desconto` sin acento como el resto de la página. Declinado: atar los `title` del manifiesto a los de `server.ts` — ADR-031 los tolera distintos por audiencia.

**Motivo:** auditoría del 2026-09-24 — la documentación llevaba desde el 2026-07-09 sin contenido nuevo pese a ~40 novedades y ocho módulos, y tres afirmaciones inducían a error operativo. La pestaña del asistente se genera para que no envejezca como el resto.

**[MCP Odoo/#113]:** cartera y DSO por cliente — fase 8 del bloque Odoo (ADR-025 §F8).
- `catalog.ts`: `res.partner.readOnlyFields = [total_due, total_overdue, days_sales_outstanding]` (computados sin store; legibles por registro, vedados en dominios/order).
- `odoo_customer_profile`: pide esas cifras en la misma lectura del contacto y responde `cartera` (`deuda_total`, `vencido`, `dias_promedio_de_pago` + `nota`: saldo contable NETO de notas de crédito, no cuadrable contra la facturación bruta, #102); el contacto no arrastra las llaves crudas. `receivableBlock()` compartido.
- **Tool nueva `odoo_receivables_ranking(limit?)`** (`tools/odoo/receivables.ts`): todos los clientes con `customer_rank > 0` (páginas de 100 — tres computados por fila contra el timeout de 25 s —, tope 1000 y un `search_count` decide si faltó alguien), filtrados a saldo > 0 y ordenados en memoria — `por_vencido` (vencido desc, luego deuda) y `mas_lentos` (DSO desc) — con totales de cartera. Responde "¿a quién le cobro primero?" y "¿quién paga más lento?".
- Recorte de la issue: "cobros sin conciliar con banco" descartado — verificado que los 82 cobros pagados tienen `is_matched = true`.
- Tests: `odoo-receivables.test.ts` (4: digestión, ranking, paginación/limit, candado del catálogo) + `odoo-sales.test.ts` extendido.

**Motivo:** el asistente ya sumaba el vencido por cliente desde facturas, pero no tenía la deuda total ni los días promedio de pago (GE 110, Andritz 88, FieldCore 74) — la base para priorizar cobranza. Reunión 2026-09-24.
- Review del PR #117: `cartera` viaja con su nota de semántica (neto de notas de crédito, moneda de la compañía) para que el modelo no reste dos veces; el ranking declara su `universo` (clientes con rank > 0); `PAGE` 200 → 100; `credit` fuera del catálogo y de la respuesta (duplicaba `deuda_total`); `cliente` nulo → "Sin nombre"; tope con `search_count`; fechas corregidas a 2026-09-24 (hora de Morelia) y conteo real de tools: 44 (el desfase de 1 venía de #109, que contó 33 de base cuando eran 32).

**[MCP Odoo/#110]:** vínculo real factura↔orden de venta — fase 7 del bloque Odoo (ADR-025 §F7).
- Exploración en vivo (`fields_get` + muestras, Odoo 19): `sale_order_count` e `invoice_ids` computados sin store (filtrarlos truena) → `readOnlyFields`; `sale_line_ids`, `invoice_lines`, `qty_to_invoice`, `narration`, `invoice_payment_term_id` y `product_uom_id` SÍ almacenados → entran como campos normales. Opción "store=True en Odoo" descartada (Online no admite módulos custom).
- `catalog.ts`: campos nuevos en `account.move`, `account.move.line`, `sale.order` y `sale.order.line`. `normalize.ts`: `htmlToText()` para `narration` (HTML de Odoo → texto).
- **Tool nueva `odoo_invoice_link_check(date_from?, date_to?, cliente?, incluir_ligadas?)`** (`tools/odoo/links.ts`): lee las facturas del periodo por páginas de 200 (tope 2000 con nota), clasifica por el vínculo real — `ligada` / `vinculo_roto` (origen sin orden ligada) / `huerfana` — y devuelve las problemáticas con `pedido_pie` (notas al pie en texto) y término de pago. Una llamada lógica; lista para la rutina programada (issue aparte).
- `odoo_invoice_detail`: `vinculo_venta` (órdenes ligadas + diagnóstico), `termino_pago`, `notas_pie`, y por línea `unidad` + `ligada_a_venta`. `odoo_sale_detail`: `facturas` ligadas (resueltas con una llamada extra solo si hay), y por línea `unidad`, `por_facturar` y `lineas_de_factura`; borrador sin folio etiquetado.
- Smoke en vivo jul–sep: 210 facturas, 194 ligadas, huérfanas de Andritz (PO solo en el pie) y vínculos rotos reales de FieldCore (F00365/F00366/F00393). Tests: `odoo-links.test.ts` (7, incl. paginación y candado del catálogo) + `odoo-documents.test.ts` extendido con las formas capturadas.
- Review del PR #116: `odoo_query` digiere `narration` con `htmlToText()` (seguía llegando HTML crudo por la primitiva; sigue filtrable); `tools/odoo/dates.ts` unifica `ISO_DATE`/`assertDateRange`/`daysAgo`/`idsOf` (antes 4 copias) y **un rango invertido es error**, no un reporte vacío que se lee como "todo en orden"; `linkDiagnosis` gana `desconocido` (sin `sale_order_count` numérico → `sin_diagnostico`, nunca falsa huérfana); al tope de 10 páginas un `search_count` decide si de verdad faltó algo (sin falso "truncada" en 2000 exactas); `clientes_encontrados` lista los partners que abarcó el `ilike`; borrador con folio `'/'` también se etiqueta; tests con `expect(encontrado)` antes del `if` y fixture `[]` corregida. Declinado: bajar `PAGE` a 100 — el smoke real con páginas de 200 respondió lejos del timeout de 25 s. Segundo round: un `search_count` no numérico cuenta como truncado (nunca "periodo completo" por accidente); `clientes_encontrados` solo cuando se filtró por cliente; `idsOf` a `normalize.ts` (no era de fechas); nota simétrica cuando una venta tiene más de 50 facturas ligadas; el mensaje del candado `readOnlyFields` ya dice "Odoo lo rechaza o devuelve 0 en silencio".
- Exploración extra para rutinas futuras (misma llave): entregas (`stock.picking`), `delivery_status`, `validity_date`, conciliación de cobros, deuda/vencido por cliente (computados → readOnly), `client_order_ref` (0/532 en uso), límites de crédito en 0, Sale Margin no instalado. Documentada en ADR-025 §F7 y repartida en issues #112–#115; fuera de este PR por decisión.

**Motivo:** la rutina de revisión "cada factura ligada a su orden" era imposible: el vínculo solo se infería por `invoice_origin` (texto), no detectaba vínculos rotos, el tope de 50 y el traversal vedado obligaban a ~8 consultas por cliente. Reunión 2026-09-24.

**[MCP/#109]:** cobertura de lectura de todos los módulos + primeras escrituras fuera del núcleo (facturas por pagar). ADR-030.
- **8 tools de lectura nuevas**, cada una reusando el helper del módulo (nunca duplicando matemática): `list_suppliers` (`paymentTermsLabel`), `list_payables`/`get_payable`/`get_payables_overview` (`summarizeMonth`, `daysUntilDue`, `describeAuditEvent`), `get_month_closing` (`buildIncomeOverview` + `monthClosing`, con `IncomeDeps` inyectables y degradado sin Odoo como la ruta), `get_cut_plan` (`tubeNetNeeds`/`plateNetNeeds` + `packBars`/`packSheets` por cada presentación capturada), `get_purchase_plan` (`buildPurchasePlan` digerido: matemática, recomendación, decisión guardada + staleness), `get_app_settings`. 33 → 43 tools.
- **2 escrituras**: `mark_payable_paid` (pagada con fecha real o de regreso a pendiente; guardas: ya pagada sin `fecha_pago` no re-sella hoy, cancelada se reactiva en la app, prefiere las pendientes al buscar por nombre) y `create_payable` (nace `pending`; vencimiento = fecha + plazo del proveedor si no se indica). Editar/borrar quedan en la app por decisión.
- `src/lib/payables.ts`: `resolvePaymentUpdate()` e `ISO_DATE` — la regla de pago ahora es UNA para el `PATCH` y la tool. `src/lib/mcp/shared.ts`: `isUuid` y `requireSingleMatch` (ninguna/varias coincidencias → error que lista candidatas); `resolveOrder` en `tools/orders.ts` (UUID o nombre/cliente), `resolveSupplier` y `resolvePayable`.
- `server.ts`: `SERVER_INSTRUCTIONS` con Proveedores, Finanzas, Corte/Planificador y Configuración; regla nueva "antes de cualquier escritura di qué vas a hacer y espera confirmación"; `BUSINESS_RULES_MD` corrige la regla del Excel URREA (pertenencia al catálogo por `catalogKey`, ya no `brand='URREA'`) y suma planificador, corte, facturas por pagar y cierre.
- **Test anti-drift `tests/mcp/coverage.test.ts`**: recorre `src/app/api/**/route.ts`, detecta las que exportan `GET` y exige mapa explícito "cubierta por tool X / fuera: motivo"; también truena si el mapa arrastra rutas inexistentes. Hallazgo colateral: `orders/[id]` no tiene `GET` (el hook lee Supabase directo, ya era deuda conocida).
- Tests nuevos: `tests/mcp/{suppliers,settings,cutting,finance,payables,purchase,coverage}.test.ts` (31) + `resolvePaymentUpdate` en `tests/lib/payables.test.ts`.
- Review del PR #111: `get_payable` resuelve el rol con `profiles.role` y a un member no le manda ni la llave `historial` (ADR-028: la existencia de la bitácora es admin-only) — se quitó toda mención a la bitácora de `SERVER_INSTRUCTIONS`, `BUSINESS_RULES_MD` y las descripciones; `get_month_closing` reporta `egresos.truncado` (mismo criterio que el overview: un corte silencioso movería el cierre); `list_suppliers` con `marca` lee ancho (1000) y corta DESPUÉS de filtrar, con `mostrados`; el test de `listPayables` con `mes` usa fechas ancladas al mes actual (las relativas cruzaban el rango los primeros/últimos días); `create_payable` dice "Contado" en vez de "0 días". Declinado: mover `num()` a `src/lib` (la ruta devuelve `null` y la tool `0`: contratos distintos de frontera).

**Motivo:** cinco módulos nuevos nunca recibieron tool y el asistente no podía responder "¿qué debo esta semana?" ni "¿cuánto tubo necesito para la orden 12?". Facturas por pagar se abre a escritura porque es un registro simbólico de gastos (Odoo tiene la facturación oficial), no mueve inventario ni dinero, y la bitácora (#100) atribuye la escritura por MCP sola vía `auth.uid()`. Reunión 2026-09-24.

**[Horas/MCP/#101]:** gráficas con referencias de jornada y lectura del módulo por MCP (ADR-029).
- **Migración `20260924055249 add_profile_shift`**: `profiles.shift` (`full_time·part_time·NULL`); datos en la nube: todos `full_time`, Tania `part_time`. RLS sin cambios. `PATCH /api/profiles/[id]` acepta `shift`; `/api/profile` y `/api/profiles` lo devuelven; Equipo lo edita.
- `src/lib/timesheet.ts`: `SHIFT_HOURS` (8/4 al día, 40/20 a la semana), `SHIFT_LABELS`, `weekChartData`, `shiftProgress`, `buildWeeklyTrend` — toda la matemática de las gráficas en lib pura.
- Componente **Chart de shadcn** (`ui/chart.tsx`; `shadcn add chart` intentó reescribir `card.tsx` con un paquete `cn` nuevo → revertido). `WeekChart`/`WeekBars` (barras por día, dos `ReferenceLine`, la de la persona sólida; checadas sin salida en color de aviso; badge cumple/faltan) y `TrendChart`/`TrendBars` (8 semanas, referencias semanales, semana visible resaltada). Cargadas con `next/dynamic` como el donut. `HoursView` pide un segundo rango de 56 días y agrupa con `buildWeeklyTrend`; la jornada sigue a la persona en pantalla.
- MCP: `src/lib/mcp/tools/hours.ts` — `get_week_hours`, `get_hours_trend`, `list_time_imports`, solo lectura y sin lógica de permisos (la RLS decide). `run()` pasa el contexto (`ctx.userId`) además del `db`. `SERVER_INSTRUCTIONS` con el bloque de Horas y la separación de `odoo_employee_directory`.
- Tests: lib (15 en timesheet), api (profiles), componentes (WeekChart/TrendChart sobre el encabezado, TeamTable, HoursView), mcp (hours: member "yo", admin por nombre, member preguntando por otro → error claro, cotas).

- Review del PR #108: un admin con jornada que miraba a alguien **sin** jornada le prestaba la suya (el `??` no distinguía "fila no encontrada" de "shift null") — corregido y con test; `formatDayMonth` debajo de `formatDayLong` (JSDoc huérfano); `resolveTarget` lee 6 para decir "más de 5 coincidencias" en vez de un conteo falso; en `get_hours_trend` el cumplimiento se llama `cumplimiento_promedio_pct`/`faltante_promedio` para que el modelo no lo frasee como "esta semana"; `shiftLineProps` compartido entre las dos gráficas.

**Motivo:** la rejilla decía cuántas horas hubo, no si eran las que tocaban; la referencia útil es la de la jornada de cada persona (Tania es de medio tiempo), por eso va en su perfil y no como constante. El MCP no tenía ninguna tool de horas; se agregan sin repetir permisos porque la policy ya dice lo mismo que la ruta.

## 2026-09-23

**[Finanzas/#100]:** filtros, fecha de pago editable y bitácora admin-only.
- **Migración `20260924031216 add_audit_events`** (ADR-028): tabla genérica `audit_events` (entidad, acción, `actor_id` FK sin cascade, `actor_name` snapshot, `data` jsonb). RLS: solo el admin lee; `authenticated` **sin INSERT** — escribe únicamente el trigger `payables_audit` → `audit_payable()` (SECURITY DEFINER, `search_path=''`, REVOKE de PUBLIC) en `created` / `status_changed` / `paid_at_changed` / `deleted`. Smoke en la nube con rollback: el member deja el evento con su nombre, lee 0 filas, no puede insertar ni llamar la función; el admin lo ve. `schema.sql`, baseline y `resetDb()` actualizados.
- `GET /api/payables`: filtros `supplier` (uuid), `minAmount`/`maxAmount` (400 si inválido o `min > max`); pasa a `requireRole` y **solo para un admin** anexa `paid_by` (último evento "marcada pagada", una consulta por página); para un member la llave no existe. Nueva `GET /api/payables/[id]/events` (`requireAdmin`).
- `PayableForm`: en edición, select de Estado + "Pagada el" (habilitada solo con Pagada; hoy por default al pasar a Pagada; se limpia al salir). El form manda `status`/`paid_at` **solo si cambiaron** — un `status: 'paid'` suelto re-sellaría hoy. Sección "Historial" solo para admin (`describeAuditEvent` en `lib/payables.ts`).
- `PayablesTable`: `payablesColumns(isAdmin)` — la columna "Pagada por" solo existe para el admin, tampoco en el picker. Página: select de proveedor y monto mín/máx en la query key del hook.
- Tests: rutas (filtros, `paid_by` solo admin, 403), form, tabla, `describeAuditEvent`, y **`tests/integration/payables-audit.integration.test.ts`** — la única prueba real del trigger y la RLS; corre en CI cuando el PR toca `supabase/**` (51 tests en verde en el PR #106).
- Review propio del PR #106 (el reviewer de CI se salta los PRs a `stg` hasta que el `claude.yml` podado llegue a `main`): **migración `20260924034509`** quita el FK `actor_id → profiles` — dentro del trigger, un JWT sin perfil tumbaba la escritura del usuario; `paid_by` solo para filas pagadas hoy (una regresada a pendiente conserva su evento viejo y diría "Pagada por"); el error de la lectura de bitácora se registra en vez de degradar en silencio; desempate por `id` en las dos lecturas; `parseAmountFilter` limpia separadores de miles (`parseFloat('1,500')` daba 1); el form conserva la fecha real en un resbalón pagada→pendiente→pagada; `PAYABLE_STATUSES` e `isUuid` compartidos en vez de cuatro copias.

**Motivo:** la pantalla se usa a diario y ya cuesta encontrar una factura; la fecha de pago solo podía sellarse como "hoy" y es la cifra del cierre real del mes; y el negocio pidió saber quién marcó pagada cada factura **sin que los members sepan que se guarda** — lo que obliga a una tabla aparte con RLS y a un trigger, porque una columna en `payables` la lee cualquiera por PostgREST.

**[CI/#104]:** poda del prompt del reviewer + red de integración para PRs de schema (auditoría del proceso).
- `.github/workflows/claude.yml`: el prompt ya NO carga copias de las reglas — el índice duplicado se había podrido (citaba el criterio viejo del Excel URREA `brand='URREA'`, la llave `normalizeCatalogCode` en vez de `catalogKey(code, brand)`, y una estructura de `src/lib/` congelada en 5 archivos). Ahora es puntero puro: leer CLAUDE.md (reglas + tablas de BD + convenciones), `business-rules.ts`, y el helper/ADR del módulo tocado. Bloqueantes ganan la regla de RLS↔ruta (#93/PR #99). Nueva sección de **re-reviews**: verificar hallazgos previos resueltos y revisar a fondo solo el delta — menos churn entre rondas.
- `.github/workflows/integration.yml` (nuevo): levanta Supabase local (CLI + Docker) en CI y corre `bun run test:integration`, SOLO cuando el PR toca `supabase/**`. Cubre lo que el mock finge (constraints, RLS/GRANT, transaccionalidad); sin `.env.test` (los helpers traen las llaves demo como fallback). No marcarlo como required check (es condicional por paths).
- `vitest.integration.config.ts`: comentario actualizado (ya no es "fuera del CI" a secas).

**Motivo:** auditoría del proceso (2026-09-23): las rondas de review eran el costo dominante del ciclo (PR #96: 3 rondas; PR #98: 7 tandas de fixes) y el prompt duplicado contradecía a CLAUDE.md — el mismo drift que el diseño "fuente única" quería evitar. El hueco de RLS sin red automática lo demostró el PR #99.

## 2026-09-21

**[Finanzas/MCP/#102]:** notas de crédito de Odoo — se informan, nunca se restan.
- `src/lib/odoo/domains.ts`: `OPEN_BALANCE_CONDITIONS` factorizado; nuevos `OPEN_CREDIT_NOTES_DOMAIN` (`out_refund`) y `OPEN_CUSTOMER_MOVES_DOMAIN` (`move_type in [out_invoice, out_refund]`). `OPEN_RECEIVABLES_DOMAIN` y `overdueDomain` sin cambios: las vencidas siguen siendo solo facturas.
- `fetchOpenReceivables` → `fetchOpenCustomerMoves`: una sola `search_read` con ambos tipos y el campo `move_type`; `OdooOpenInvoice.moveType`; `Math.abs()` sobre `amount_total`/`amount_residual`. Key del Data Cache `open-receivables` → `open-moves` (las entradas viejas no traen `moveType`).
- `src/lib/income.ts`: `splitReceivables` salta las `out_refund`; `summarizeCreditNotes` (`creditNotesTotal/Count`, `creditNoteCurrencies`); `IncomeOverviewResponse.creditNotes`. Test que fija que el por cobrar con notas es idéntico al por cobrar sin ellas.
- `FinanceOverview`: card "Notas de crédito sin aplicar" (total · conteo, filas cliente · folio / fecha · saldo, nota fija de que no se resta); el aviso de truncado menciona a las notas.
- MCP: `odoo_overdue_invoices` (+1 `read_group`, `notas_credito_sin_aplicar` por cliente), `odoo_invoices_summary` (+1 `read_group`, `notas_credito` del periodo), `odoo_customer_profile` (+1 `search_read`, notas abiertas del cliente). Descripciones y `SERVER_INSTRUCTIONS` con la regla de no netear.
- ADR-027 §8.
- Review del PR #103: el saldo a favor de `odoo_customer_profile` sale de un `read_group` (exacto) y la lista de 10 va aparte con `lista_completa`; `creditNotesByCustomer` se comparte entre tools; los tres puntos del MCP aplican `Math.abs` como el loader; la card lleva `currencyNote(creditNoteCurrencies)` y ordena por fecha de emisión descendente; descripciones distinguen "abiertas a hoy" de "emitidas en el periodo".

**Motivo:** el "Vencido por cobrar" ($491,063 en producción) sale de un dominio que solo lee `out_invoice`; las notas de crédito no entraban por ningún lado y nadie sabía si había saldo a favor compensando parte de eso. El negocio decidió (2026-09-21) informarlas aparte y **no** descontarlas: no se sabe si el cliente usará la nota o si se aplicará a una factura, y un neteo inventado confundiría la deuda real.

## 2026-09-16

**[Horas/Finanzas/#93 #94]:** correcciones del review del PR #99 (`stg` → `main`).
- **Migración `20260916190052 tighten_profiles_and_imports_rls`**: `profiles` pasa de `SELECT USING (true)` a fila propia o admin, y `time_imports` a solo admin. El MCP lee con el token del usuario, así que la policy abierta dejaba a cualquier miembro leer rol e id de checador de todo el equipo sin pasar por `requireAdmin`. Smoke en la nube: Tania ve 1 perfil y 0 cargas; Diego ve los 5 perfiles. `GET /api/time-entries/imports` también pasa a `requireAdmin`.
- En la misma migración, el conteo `v_total` de `import_time_entries` deduplica sobre valores casteados: `8:05` y `08:05` ya son la misma pareja y `skipped_edited` no se infla si algún día llega una hora sin normalizar.
- **El Cierre del mes lleva el aviso de moneda**: suma los cobros sin convertir, y era la única tarjeta de ingresos sin la nota "incluye USD sin convertir".
- `todayIso()` y `daysSince()` usan la fecha de Morelia: el MCP y la ruta de ingresos comparten `overdueDomain` y ya no discrepan de 18:00 a medianoche.
- El doble costo del botón Actualizar queda como decisión (ADR-027 §7), ahora explicado en el propio handler.
- Tests: 403 de la bitácora para member, nota de moneda en el cierre, reloj de Morelia, y en integración la RLS nueva y el conteo casteado.

**Motivo:** tres advertencias y tres sugerencias del revisor de IA sobre el PR a `main`. La de RLS fija el precedente de la primera tabla con permiso por persona: la policy dice lo mismo que la ruta.

## 2026-09-09

**[Finanzas/#94]:** módulo de ingresos leído desde Odoo — la otra mitad del overview y el cierre del mes.
- **Sin tabla nueva.** `src/lib/odoo/income.ts`: `fetchMonthCollections()` (`account.payment` inbound, `state in (in_process, paid)`, `date` en `[mes-01, mesSiguiente-01)`) y `fetchOpenReceivables()` (`OPEN_RECEIVABLES_DOMAIN`, ahora en `src/lib/odoo/domains.ts` y compartido con las tools MCP — `overdueDomain` se movió ahí). Caller inyectado y catálogo como frontera, igual que las tools; `currency_id` entró al catálogo de `account.payment`. 2 llamadas por mes, `limit 500` con flag `truncated`.
- **Matemática pura** en `src/lib/income.ts` (reloj inyectado, patrón `payables.ts`): `summarizeCollections`, `splitReceivables(rows, today)` (vence `< hoy` → vencido; hoy, después o sin fecha → por cobrar; usa `amount_residual`), `monthClosing` (real = cobrado − pagado, proyectado = real − pendientes; negativo válido), `foreignCurrencies`, y los builders `buildIncomeOverview`/`incomeUnavailable`.
- **Rutas** `GET /api/finance/income?month=` y `POST /api/finance/income/refresh`. Primer uso del Data Cache de Next: `src/lib/odoo/income-cache.ts` es el único archivo que importa `next/cache` (`unstable_cache`, tag `finance-income`, 900 s). `revalidateTag(tag, { expire: 0 })` — el segundo argumento es obligatorio en Next 16 y `'max'` sería stale-while-revalidate. Odoo ausente/caído → **200 con `income: null`** y `unavailable.reason`; solo un error ajeno da 500. `maxDuration = 60`.
- **UI**: `FinanceOverview` gana la sección "Ingresos (Odoo)" (Cobrado del mes, Por cobrar, Vencido por cobrar, Cierre del mes con proyectado), "Actualizado hace X" + botón Actualizar (`useRefreshIncome` siembra el mes y marca los demás stale sin refetch), estado "Ingresos no disponibles", y la tarjeta "Cobros de {mes}" con etiqueta de moneda ≠ MXN. El cierre se calcula en el cliente con los dos hooks para que la ruta de ingresos sea solo-Odoo. Todo el equipo lo ve (`requireAuth`).
- **Tests**: `tests/lib/odoo-income.test.ts` (dominios exactos, campos ⊆ catálogo, truncado, `overdueDomain` tras el movimiento), `tests/lib/income.test.ts`, `tests/api/finance-income.test.ts` (401/400, no configurado sin tocar Odoo, `OdooError` → 200 degradado, error ajeno → 500, refresh purga con `{ expire: 0 }`; `next/cache` y el singleton `callOdoo` mockeados conservando `OdooError`), `tests/components/FinanceOverview.test.tsx`, y 2 filas en `auth-guards`. Los tests MCP de accounting/sales siguen verdes sin tocarse.
- Fuera: simulación de mover pagos entre meses (único pendiente de #84 fase 2), tools MCP nuevas, histórico anual.

**Motivo:** issue #94 — los ingresos ya viven en Odoo y duplicarlos sería mantener dos verdades; sin ellos el overview no podía decir cómo cierra el mes, que es para lo que existe. Decisiones en [[04-Decisiones-Tecnicas/ADR-027-Ingresos-desde-Odoo]].

**[Finanzas/#94]:** correcciones del code review del PR #98.
- **`partner_type = customer` en el dominio de cobros** (y en el catálogo de `account.payment`): un reembolso recibido de un proveedor o una transferencia interna también son `inbound` y entraban al cobrado del mes.
- **Moneda en los dos lados**: `currency_id` también se lee de las facturas abiertas; `foreignCurrencies` del resumen une cobros y facturas y el badge "Incluye USD sin convertir" pasó al encabezado de ingresos.
- **Un GET fallido ya no pinta $0.00**: `FinanceOverview` toma `isError` del query y muestra "Ingresos no disponibles" con reintento, como `WeekGrid`.
- `src/lib/month.ts` nuevo (`ISODate`, `ISO_MONTH`, `monthOf`, `nextMonth`, `monthRange`): `payables.ts` los re-exporta, los loaders de `lib/odoo/` dejan de importar de la capa app, `monthRange` deja de ser código muerto y `ISO_MONTH` sale de `route.ts` (también en `payables/overview`). Truncado partido en `collectionsTruncated`/`receivablesTruncated` para que el aviso hable del renglón correcto.

**Motivo:** tres advertencias y seis sugerencias del revisor de IA; la del `partner_type` era la que podía mover el cierre del mes.

**[Finanzas/#94]:** segunda pasada del review del PR #98.
- **El proyectado resta también las vencidas de meses previos.** `summarizeMonth` gana `carryOverTotal/Count` (vencidas con vencimiento anterior al mes; no se traslapa con `pendingTotal`) y el cierre proyectado = real − pendientes del mes − arrastre. Decisión del usuario: lo atrasado también hay que pagarlo.
- "Por cobrar" y "Vencido por cobrar" dicen "al día de hoy": son una foto, no cambian al navegar de mes.
- Monedas extranjeras por lado (`collectionCurrencies` / `receivableCurrencies`): una factura vieja en USD ya no pinta el badge en todos los meses. Re-export muerto de `overdueDomain` eliminado de la tool de accounting.

**[Finanzas/#94]:** tercera pasada del review del PR #98.
- La resta del arrastre vencido pasa del componente a `monthClosing({ collected, paid, pending, carryOver })` en `src/lib/income.ts`, con doc comments y test propios: la fórmula del proyectado vive en un solo lugar.
- `splitReceivables` devuelve `receivableCurrencies` y `overdueCurrencies` por separado: una factura vencida en USD ya no etiqueta la tarjeta de "Por cobrar".
- ADR-027 §6 documenta el límite al navegar a meses futuros (lo que vence entre hoy y ese mes no entra al proyectado).

**[Finanzas/#94]:** cuarta pasada del review del PR #98.
- `GET /api/payables/overview` expone `pendingTruncated` (`count: 'exact'` contra el límite de 1000): las pendientes alimentan el arrastre y el proyectado, así que un corte silencioso mentiría igual que del lado Odoo. El overview lo avisa.
- La ruta usa `monthRange()` para la frontera de `paid_at`; `splitReceivables` junta monedas con `Set` en el mismo bucle en vez de copiar filas.

**[Finanzas/#94]:** quinta pasada del review del PR #98.
- El aviso de egresos truncados se pintaba bajo el encabezado "Ingresos (Odoo)"; ahora vive debajo de la rejilla de egresos, que es de lo que habla.
- `paidTruncated` por simetría: las pagadas del mes alimentan el cierre real y eran la única de las cuatro lecturas sin bandera. Test del borde exacto (1000 filas no se marcan truncadas).
- ADR-027 §7 explica por qué el criterio de truncado difiere entre Postgres (exacto) y Odoo (conservador).

**[Finanzas/#94]:** sexta pasada del review del PR #98.
- La lectura de pagadas del mes ordena por `paid_at` antes del `limit`: sin `ORDER BY`, un truncado devolvería un subconjunto arbitrario y el cierre real podría bailar entre dos cargas del mismo mes.
- Los cuatro avisos de truncado dejan de escribir el número a mano (1000/500): los límites viven en constantes y el texto habría mentido al subirlos. Dicen qué pasó y qué cifra puede quedar corta, que es lo accionable.

**[Finanzas/#94]:** séptima pasada del review del PR #98.
- **El lado de egresos también maneja `isError`**, como ya lo hacía el de ingresos: si ese GET falla se muestra "Egresos no disponibles" en vez de cuatro tarjetas en $0.00. Y el **Cierre del mes** pinta un guion cuando le falta cualquiera de las dos mitades, en vez de un cero que se lee como un mes cerrado en ceros — es la cifra con la que se decide el pago de impuestos.
- Las dos lecturas truncables desempatan por `id`: `due_date` y `paid_at` son `date` no únicas, así que las filas que empatan en la fecha de corte seguían entrando en orden arbitrario. Test que fija el orden de ambas.

**[Finanzas/#94]:** octava pasada del review del PR #98.
- **La tarjeta de vencimientos ya no afirma que el mes está limpio** cuando la lectura falló: decía "Sin facturas pendientes" justo debajo del aviso de error, y alguien podía cerrar la pantalla creyendo que no debe nada.
- **Una sola semántica de error para las dos mitades**: "no disponible" es *no hay nada que mostrar*; si un refetch falla con datos en caché, los datos se conservan y se marcan como de la última carga buena. Antes se ocultaban cifras que sí se habían cargado bien, y en el caso de egresos la pantalla decía "no disponibles" mientras el cierre seguía usándolas.

## 2026-09-08

**[Horas/#93]:** módulo de registro de horas desde el reporte del checador NGTeco, con el primer permiso por persona del sistema.
- **Migración `20260909031135 add_profiles_and_time_entries`** (aplicada en la nube; baseline y `schema.sql` en el mismo commit): `profiles` 1:1 con `auth.users` (trigger `handle_new_user` SECURITY DEFINER + backfill), `is_admin()` (DEFINER, `search_path = ''`), `time_entries` (una fila por pareja; `source_clock_in` inmutable como llave `UNIQUE` de idempotencia; rastro `edited_by/edited_at/original`), `time_imports` y la RPC transaccional `import_time_entries` (INVOKER; `ON CONFLICT ... DO UPDATE ... WHERE edited_at IS NULL`). RLS: SELECT propio o admin, escrituras solo admin; GRANTs sin `anon`. Paso de datos solo en la nube: Axl y Diego admin; ids de checador Diego 1, Santiago 2, Canales 3, Tania 5. Smoke por MCP como Tania (0 filas ajenas, INSERT → 42501) y como Diego (la RPC inserta y luego actualiza sin duplicar).
- **Lib `src/lib/timesheet.ts`**: `parseNgtecoReport()` (máquina de estados sobre la matriz de celdas: "Período de pago", `Nombre (id)`, filas de continuación que heredan la fecha, salida vacía, celdas numéricas de Excel → `HH:MM`/ISO), `minutesBetween`, `formatDuration` (> 24 h), `weekBounds` lunes→domingo, `buildWeekView()` (7 días, totales derivados, nunca persistidos). La columna "Tiempo de trabajo" del reporte se ignora y se recalcula: los totales pueden diferir ±1 min porque el checador cuenta segundos que no imprime.
- **API**: `requireAdmin()` en `api-helpers.ts`; `/api/profile`, `/api/profiles[/id]` (último admin protegido, 23505 → 400), `/api/time-entries` (un member ignora `?user=` y recibe lo propio; `time` normalizado a `HH:MM`), `[id]` PATCH (sella rastro, `original` una sola vez, jamás `source_clock_in`) y DELETE, `/import` (multipart → parser → mapeo por `clock_employee_id` → RPC; los no mapeados se reportan, no bloquean) e `/imports`.
- **UI**: sección **Horas** en el sidebar (`Mi semana` para todos; `Importar reporte` y `Equipo` solo admins — `useProfile().isAdmin`, y el server contesta 403 igual). `WeekGrid` (7 filas, "sin salida" en ámbar, marcador de editado con tooltip "el checador decía A–B"), stepper de semana, selector de empleado (admin), `TimeEntryForm` (corregir/alta manual/eliminar), `TimeImportPanel` (reusa `FileUploader` con prop `hint` nueva; panel con insertadas/actualizadas/saltadas por edición/no mapeados con link a Equipo/avisos; bitácora), `TeamTable`. Iconos `CalendarClock` y `Users` directos de lucide.
- **Tests**: `tests/lib/timesheet.test.ts` (12), `tests/api/{profiles,time-entries}.test.ts` (rol simulado con función sobre `'profiles.select'`; la RPC verificada por `_rpcCalls` con un `.xls` BIFF real vía `makeExcelRequestFromRows`), `auth-guards` con tabla de 401 y tabla hermana de **403**, componentes `WeekGrid`/`HoursView`/`TimeImportPanel`, y `tests/integration/time-entries.integration.test.ts` — la única prueba real de RLS (member: SELECT solo propio, INSERT 42501, UPDATE/DELETE 0 filas; la RPC respeta filas editadas). Seed local con segundo usuario `member@dymmsa.local`; `resetDb()` nunca trunca `profiles`. Docker no estaba disponible al escribirlo: **correr `bun run test:integration` antes del merge a `main`**.
- Fuera de v1: captura de rutina, turnos nocturnos, export a Excel, tools MCP, tour guiado.

**Motivo:** issue #93 — las horas solo existían en el `.xls` semanal; se quería histórico consultable con dos niveles de acceso. Decisiones en [[04-Decisiones-Tecnicas/ADR-026-Perfiles-y-Permisos-por-Rol]].

**[Horas/#93]:** correcciones del code review del PR #96.
- **Migración `20260909162418 import_time_entries_deterministic_dedupe`**: el `DISTINCT ON` de la RPC no tenía `ORDER BY`, así que una pareja repetida en el reporte (una con salida y otra sin) conservaba una fila arbitraria entre corridas. Ahora `ORDER BY ... clock_out DESC NULLS LAST`: sobrevive la completa. Test de integración con el par en ambos órdenes.
- **Una nota sola ya no congela la fila.** `edited_at` se sellaba con cualquier cambio, y como el import salta filas con `edited_at`, una pareja con solo "olvidó checar salida" jamás recibiría la salida que el checador reportara después. Ahora solo un cambio de `clock_in`/`clock_out` sella el rastro y escribe `original`.
- El tooltip "Editado por X el …" cortaba el `timestamptz` a los 10 caracteres → día UTC (entre las 18:00 y la medianoche en Morelia pintaba el día siguiente). Ahora `formatAbsolute()`.
- `requireRole()` en `api-helpers.ts` (auth + perfil, sin gate); `requireAdmin()` se construye encima y `GET /api/time-entries` deja de repetir a mano el `select('role')`. `normalizeEntryTimes()` pasa de `route.ts` a `src/lib/timesheet.ts`. El parser distingue "hora de entrada inválida" de "salida sin entrada". `DELETE` de un id inexistente → 404.
- Documentado: borrar un usuario de `auth.users` con checadas falla con 23503 (la baja es desactivar); `week` del GET solo es válido con un rango lunes→domingo; `time_imports` la lee cualquier `authenticated` a propósito. Se descartó `raw: true` en `sheet_to_json`: el `.xls` real llega como texto y el fixture numérico ya cubre las ramas de `cellText()` por si cambia.

**Motivo:** cinco advertencias y seis sugerencias del revisor de IA; las tres que pedía antes de `main` (determinismo, nota que congela, fecha UTC) van con test o con helper compartido.

**[Horas/#93]:** segunda pasada del review del PR #96.
- La migración `import_time_entries_deterministic_dedupe` ya está aplicada en la nube (versión en `migrations-log.md`); `schema.sql` y la BD real vuelven a coincidir.
- **Una pareja con salida anterior a la entrada ya no tumba el import.** El CHECK `time_entries_out_after_in` la rechazaba con 23514 y, como la RPC es transaccional, se perdía el archivo completo con un 500 genérico. El handler la filtra con un aviso `Salida antes de la entrada: <nombre> <fecha> <in>–<out>` y, como fallback, mapea 23514 → 400 (ADR-009). Tope de 5 MB por archivo.
- `GET /api/time-entries` devuelve `week: null` cuando `from..to` no es exactamente lunes→domingo, en vez de una semana con total engañoso. El panel de import avisa con `toast.warning` cuando no entró ninguna checada.

## 2026-09-07

**[Proveedores/Finanzas/#92]:** correcciones del code review del PR #95.
- `DateFormatPicker` valida `isDateFormat` también al LEER: `formatDay` ya caía al default, pero el trigger del Select quedaba vacío porque ningún `SelectItem` empataba con un formato retirado.
- **0 días de crédito ES contado.** `paymentTermsLabel(0)` devolvía `"0 días"`; ahora `<= 0` cae a `"Contado"`, así que una fila guardada con 0 desde la API o el MCP también se lee bien. En el formulario el 0 se guarda como `null`, abre en Contado y ya no arranca en "Otro…".
- `PayableForm` deja de duplicar la etiqueta con un ternario que además pintaba `contado` en minúscula, inconsistente con la tabla de proveedores.
- Tests: `paymentTermsLabel(0)`, el selector con un formato retirado, y los dos casos del 0 en el formulario. El test del 0 destapó un cabo suelto propio — el `defaultValues` seguía mandando `'0'` al Select y lo dejaba vacío.

**Motivo:** una advertencia y dos sugerencias del revisor de IA; la del 0 se arregló en la lib y no solo en el formulario, porque el dato puede entrar por tres caminos.

**[Convención]:** los comentarios del código pasan a inglés, cortos y solo el WHY.
- Barrido completo de `src/` y `tests/`: 305 archivos, de ~2,945 líneas de comentario a ~1,722 (-42%). Se fueron las bandas decorativas de sección y el JSDoc que solo reformulaba la firma; se conservaron —acortadas— las trampas que cuestan una sesión redescubrir (`/jsonrpc` muerto en Odoo Online 21.1 y su cola serializada, los `l10n_mx_edi_*` de pagos computando `false`, `${month}-31` con 22008, el excedente de recepción por delta, el cruce estricto `(code, brand)`, los fondos opacos por `bg-inherit`, el guard de open-redirect de `proxy.ts`).
- NO se tradujo el texto que ve el usuario (labels, toasts, mensajes de error de la API) ni los nombres de los tests, que son strings. La regla quedó escrita en CLAUDE.md.
- **Verificación**: cada archivo se transpiló con `removeComments` en su versión de HEAD y en la actual, normalizando finales de línea, y se compararon. De 323 archivos, los únicos con código alterado fueron los 11 de #92. Conteo de directivas funcionales (`eslint-disable`, `@ts-expect-error`…): 48 antes, 48 después.

**Motivo:** CLAUDE.md ya pedía "todo en inglés" y comentarios de máximo 2 líneas, pero los comentarios habían quedado fuera de esa regla y arrastraban narrativa que pertenece a CLAUDE.md y a los ADRs.

**[Proveedores/Finanzas/#92]:** plazo de pago como Select de presets y formato de fecha elegible.
- `PAYMENT_TERM_PRESETS` + `paymentTermsLabel()` en `src/lib/payables.ts` (7/15/30/60/90 → "1 semana"…"3 meses"; null → "Contado"; otro → "N días"). Sin cambio de esquema: `payment_terms_days` sigue INTEGER nullable.
- `SupplierForm`: Select con Contado, los presets y "Otro…" (input libre). Centinelas `__cash__`/`__other__` porque Radix Select no admite value vacío. El caso borde de la issue: un proveedor guardado con un valor fuera de la lista (45) abre en "Otro…" con su número; y "Otro…" con el input vacío no guarda (`setError` en submit) — evita degradar a contado en silencio. `SuppliersTable` y el select de `PayableForm` pintan la etiqueta con `paymentTermsLabel`.
- `formatDay(iso, formato)` + `DATE_FORMATS` en `format.ts` (`long`, `short`, `dd-mm-yyyy`, `dd/mm/yyyy`, `yyyy-mm-dd`). Los numéricos se arman del string; `short` y `long` van por `Intl` anclado en UTC — misma razón que `formatDayLong`, ninguna fecha `date` pasa por `new Date()` crudo.
- Store `dateFormatStore` (`dymmsa-date-format`, Zustand persist) + hook `useDateFormat()` (default hasta `useMounted`, como `useVisibleColumns`, para no desalinear SSR e hidratación) + `DateFormatPicker` en la barra de Facturas por pagar. `PayablesTable` (3 columnas) y `FinanceOverview` (vencimientos por semana) consumen el hook. `resetStores()` de los tests resetea el store nuevo.
- Tests: `SupplierForm.test.tsx` (nuevo: preset, contado, "Otro…" con y sin número, y el caso borde del 45), `DateFormat.test.tsx` (picker→hook, persistencia bajo la key, formato desconocido cae al default), `formatDay` y `paymentTermsLabel` en lib.

**Motivo:** ajustes de la reunión del 2026-09-06. Los plazos reales son un puñado y teclearlos invitaba a dedazos; el formato de fecha corto facilita cotejar contra otros sistemas.

## 2026-09-06

**[Finanzas/#84]:** correcciones del code review antes del merge.
- **Bloqueante**: `GET /api/payables/overview` filtraba las pagadas con `.lte('paid_at', '${month}-31')`. En meses de 30 días —septiembre incluido, que es el default del selector— Postgres recibía una fecha inexistente y respondía `22008`, así que `/dashboard/finance` salía en blanco. `nextMonth()` sube de la ruta a `src/lib/payables.ts` (exportada, con test) y ambas rutas usan la frontera exclusiva `.lt()`. El test de la ruta usaba `2026-10`, justo el mes de 31 días que NO falla; ahora hay uno con `2026-09`.
- **`todayInMexico()`** en `format.ts` (`Intl` sobre `America/Mexico_City`) reemplaza a `formatISODate()` en las cuatro superficies donde el día ES el dato: el sello de `paid_at`, el semáforo de vencimiento, el mes por defecto del overview y la fecha de factura del alta. `formatISODate` serializa en UTC y Vercel corre en UTC, así que de las 18:00 a la medianoche de Morelia la app ya estaba en el día siguiente: una factura que vencía hoy se pintaba "vencida hace 1 día" y el botón ✓ sellaba el pago con la fecha de mañana. Los usos previos del helper son nombres de archivo de Excel, donde el corrimiento era inocuo.
- `weekOfMonth` deja de estar duplicada: `FinanceOverview` la importaba reimplementada en línea, así que un cambio en el corte de semana habría desalineado en silencio los buckets (server) contra las filas (cliente).

- **Segundo pase:** el día estaba corregido al ESCRIBIR pero seguía mal al LEER. `formatAbsolute` hace `new Date('2026-09-15')`, que es medianoche UTC, así que en Morelia las columnas `date` se pintaban un día antes y con una hora inventada: la misma fila decía "14 de septiembre de 2026, 18:00 · vence hoy", y al marcar pagada la columna mostraba ayer. Nueva `formatDayLong()` en `format.ts` (ancla y formatea en UTC — una fecha sin hora no tiene zona) en las 4 lecturas; `formatAbsolute` se queda para los timestamps reales, que es donde se usaba.
- `normalizeLabel` se exporta de `vault-changelog.ts` y la pestaña de Actividad la consume: `areaClass` la reimplementaba, y de esa copia dependían las llaves de `AREA_TINT`.

**Motivo:** los tres los levantó el revisor de IA en el PR #90. El primero rompía la pantalla el mismo mes en que se abre; los otros dos son datos que el usuario lee como verdad financiera.

## 2026-09-05

**[Novedades]:** pestaña **Actividad** — la bóveda `06-Changelog/` se publica en la app.
- `src/lib/vault-changelog.ts` (parser puro): tolera los dos formatos que conviven en la bóveda — `**[Área/#NN]:**` de los meses recientes y `**Etiqueta:**` de los primeros. `**Motivo:**` se pega al bloque abierto; `**Total:**` es del DÍA (colgarlo del último bloque se lo atribuiría solo a ese). Normaliza las migraciones (el folio pasa al título y el badge queda "Migración") y degrada a título las etiquetas de más de 24 caracteres, que como badge rompen el renglón.
- `groupActivityByMonth` agrupa por el mes de la FECHA, no por el archivo: `2026-04.md` arrastra días de marzo y agruparlos por nombre de archivo los mandaría a abril.
- Ruta `/dashboard/changelog/actividad` (Server Component): timeline mes → día → bloques, badge de área tintado por familia (Feature/Fix/Test/Refactor/Migración…), chips de issue ligados a `/dashboard/tasks/N`, detalle plegado en `<details>` nativo —abierto solo en el mes más reciente— y el Motivo destacado.
- `ChangelogTabs`: Novedades y Actividad son dos RUTAS con aspecto de pestañas, no un tab con estado. Así ambas se quedan del lado del servidor y los 108 KB de bitácora nunca viajan al cliente.
- `RichText` compartido por las dos vistas: `código`, **negritas**, `[[wikilinks]]` de la bóveda (se muestran por su nombre) y `#NNN` ligado a la tarea. Novedades lo estrena y de paso deja de imprimir los asteriscos literales, bug que arrastraba desde que existe la página.
- `next.config.ts`: `outputFileTracingIncludes` para la ruta nueva. Las dos rutas son `ƒ` (se renderizan por request), así que sin el trace la bóveda no entra al bundle y la página saldría vacía en producción; la lectura va en try/catch para degradar a vacío en vez de romper.
- Icono `History` (re-export directo de lucide, sin equivalente animado).
- Tests: 10 del parser (ambos formatos, ruido, agrupación por fecha) + 5 de `RichText`. **991 en total.**

**Motivo:** el changelog de usuario solo registra lo que se nota desde la app, así que la plomería (MCP, Odoo, refactors, tests) era invisible; la bitácora técnica ya existía y se escribía en cada cambio, solo le faltaba superficie. Se evaluó alimentar la vista con los commits y se descartó: 601 commits contra 32 novedades (19 a 1), en inglés, con 145 de puro `docs`/`test`/`refactor`/`ci`, y en Vercel no hay `.git` en runtime — habría que ampliar el scope del PAT para peor señal.

## 2026-09-02

**[Finanzas/#84]:** módulo Finanzas fase 1 — facturas por pagar + overview de egresos.
- **Migración `20260902043910 add_finance_payables`**: columna `payment_terms_days` en `suppliers` (INTEGER nullable, CHECK ≥ 0; NULL = contado) y tabla `payables` (supplier FK **sin cascade** — borrar proveedor con facturas → 23503 → 400 —, concept/amount/invoice_date/due_date, status `pending|paid|cancelled`, `paid_at` = fecha REAL de pago). Índice `(status, due_date)`, trigger moddatetime, RLS + policy authenticated.
- `src/lib/payables.ts` (patrón purchase-plan/cut-plan, NO business-rules): `dueDateFrom` (aritmética UTC, sin DST), `summarizeMonth` (pendiente del mes por semana de vencimiento; vencidas cuentan aunque vengan de meses previos; pagadas por mes de `paid_at`, no de vencimiento; canceladas fuera), `daysUntilDue`. Reloj SIEMPRE por parámetro.
- API: `GET/POST /api/payables` (paginado, filtros status + mes de VENCIMIENTO, sort whitelisted, embed del proveedor), `PATCH/DELETE /api/payables/[id]` (regla de pago: →paid registra `paid_at` con default hoy, →pending lo limpia; el POST ignora status del cliente — siempre nace pendiente), `GET /api/payables/overview?month=`. Todo con `requireAuth()`.
- Proveedores: campo "Plazo de pago (días)" en `SupplierForm` (vacío → null), columna "Plazo" en la tabla, validación entero ≥ 0 en POST/PATCH, y DELETE mapea el 23503 de payables a 400 descriptivo.
- UI: sección **Finanzas** en el sidebar (Overview + Facturas por pagar; icono `Receipt` = re-export lucide, sin equivalente animado). `PayablesTable` con el patrón completo (columnas redimensionables, acciones fijas, ✓ marcar pagada / ↩ regresar a pendiente); `PayableForm` pre-llena el vencimiento (proveedor.plazo + fecha factura) hasta que el usuario lo toca; `FinanceOverview` con selector ◀ mes ▶, 4 `MetricCard` (se agregó el tono `red` al colorMap) y vencimientos por semana.
- Tests: lib pura (10), API payables (12) + auth-guards (5 rutas nuevas), `PayableForm` (pre-llenado y override manual), plazo en suppliers API, y `payables` en el anti-drift de tablas redimensionables.

**Motivo:** los gastos se llevaban en una tabla manual sin vencimientos. Fase 1 decidida con el usuario: solo egresos y captura propia (Odoo únicamente factura a clientes); la proyección del cierre ± con ingresos y la simulación fiscal de mover pagos entre meses quedan para la fase 2.
