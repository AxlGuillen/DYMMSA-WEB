# Changelog técnico — Agosto 2026

## 2026-08-11

**[MCP/Odoo]:** Bloque Odoo Fase 1 — Contabilidad (issue #65, [[04-Decisiones-Tecnicas/ADR-025-Bloque-Odoo-MCP]]).
- `src/lib/odoo/`: `client.ts` (JSON-2 con cola serializada + backoff 429 — rate limit Odoo Online 1 req/s sin paralelas), `env.ts` (opcional, no valida al importar), `catalog.ts` (frontera: modelos/campos allowlisted, también en filtros), `normalize.ts` (many2one→nombre, false→null, __domain fuera, daysSince).
- Tools MCP en `src/lib/mcp/tools/odoo/accounting.ts`: `odoo_query`, `odoo_aggregate` (primitivas), `odoo_overdue_invoices`, `odoo_invoices_summary` (curadas). Registro en `server.ts` + línea Odoo en las reglas de negocio del recurso MCP.
- Health: check `odoo` (search_count autenticado barato; skip sin env, fail → degraded).
- Tests: `tests/lib/odoo-client.test.ts` (cola nunca paraleliza, espaciado, 429 retry-once, errores), `tests/mcp/odoo-accounting.test.ts` (catálogo como frontera, normalización, formas reales capturadas), health extendido. Smoke test en vivo contra la instancia real OK.
- Env nuevos (server): `ODOO_URL`, `ODOO_API_KEY`, `ODOO_DB` (opcional).

**[MCP/Odoo]:** Bloque Odoo Fase 2 — Contactos + Ventas (misma rama/PR).
- Catálogo: `res.partner` (sin `mobile` — no existe en Odoo 19) y `sale.order` (`date_order` DATETIME → rangos expandidos al día completo).
- Tools: `odoo_sales_summary` (por estado/cliente/vendedor/mes; default solo confirmadas) y `odoo_customer_profile` (expediente en 4 llamadas: contacto+RFC, ventas, facturación, vencidas; desambigua con lista de coincidencias).
- `normalizeGroups` descarta grupos count=0 (Odoo devuelve todas las opciones de un selection aunque el dominio las excluya).
- Tests `tests/mcp/odoo-sales.test.ts`; smoke en vivo OK (julio: $890,263.77 en 69 ventas confirmadas).

**[MCP/Odoo]:** Bloque Odoo Fases 3 y 4 — Inventario, Empleados y Flotilla (cierre del bloque).
- Catálogo: `product.product` (SIN `qty_available`: computado no-almacenado, Odoo revienta al filtrar/ordenar → la verdad es `stock.quant`), `stock.quant`, `hr.employee` (solo directorio laboral), `fleet.vehicle` + bitácora de servicios.
- Tools: `odoo_stock_check` (1 read_group, suma ubicaciones, ceros como conteo), `odoo_employee_directory`, `odoo_fleet_status` (bitácora vacía se dice explícita).
- Tests `tests/mcp/odoo-operations.test.ts` (incluye el candado de nómina: wage/contract_id/birthday rechazados); smoke en vivo OK (57 matches "punta", 6 empleados, 7 vehículos).

**[MCP/Odoo]:** Endurecimiento del catálogo (review PR #66).
- `assertDomainAllowed` rechaza traversal por relación en dominios (`partner_id.vat` pasaba validando solo el base y Odoo SÍ filtra por él — oracle de inferencia verificado en vivo).
- `odooQuery` valida TODAS las columnas del `order` (antes solo la primera).
- `campo:count` en `odoo_aggregate` confirmado contra la instancia real (funciona; se queda).

**Motivo:** dar a Claude lectura de la facturación oficial para revisiones automáticas (cartera vencida, cuadres) sin tocar Odoo ni cruzarlo con el core. `/jsonrpc` deprecado (muere en Online 21.1) → JSON-2 desde el día uno.

## 2026-08-09

**[Tours]:** Cuarto flujo — planificador de compra.
- `src/lib/tours/purchase-planner.ts`: 8 pasos anclados a `data-tour="plan-*"` en `PurchasePlanner` (resumen económico → umbrales → toggle de vistas → grupos con matemática → anatomía de un grupo → compra local → Excel → guardar). Los pasos del footer fijo llevan `side: 'top'` para que el popover no quede fuera de pantalla.
- Registro `'purchase-planner'` en `TourButton`; botón en el header del componente (el route shell `planner/page.tsx` no tiene header propio, igual que en corte).
- Anclas: grid de `PlanOverview`, trigger de `ThresholdsPopover`, toggle de vista, Cards URREA/local, root de `GroupRow` (se repite por grupo — `resolveVisible` toma el primero), div de botones Excel y botón guardar.
- Tests: anti-drift + arranque de driver.js mockeado en `PurchasePlanner.test.tsx` (fixture con grupo en catálogo + grupo local para que los 8 bloques existan).

**Motivo:** el planificador es la pantalla con más carga conceptual (ADR-018: mayoreo vs menudeo, dinero parado, staleness) y el ADR-024 la tenía como siguiente candidata.

## 2026-08-04

**[Tours]:** Vistas guiadas con driver.js (issue #52) — primer módulo: planificador de corte.
- `bun add driver.js` (v1.8.0, sin dependencias transitivas relevantes).
- `src/lib/tours/index.ts`: `startOverview()` — config compartida en español, `popoverClass: 'dymmsa-tour'`, filtra pasos cuyo selector no está en el DOM (secciones condicionales se saltan en vez de mostrar popover huérfano).
- `src/lib/tours/cut-planner.ts`: 8 pasos de overview anclados a `data-tour="cut-*"` en `CutPlanner`.
- Botón "Vista guiada" (`CircleHelp`) en el header de `/dashboard/orders/[id]/cutting`.
- Tema del popover en `globals.css` con tokens del sistema (light/dark).
- Tests: anti-drift de selectores (cada `data-tour` del tour debe existir con el fixture completo) + arranque de driver.js mockeado (10 tests en `CutPlanner.test.tsx`).

**Motivo:** el diseño de "dos momentos" del módulo de corte (ADR-022) confundió al propio dueño en su primer uso; los overviews explican los bloques sin volverse tutoriales frágiles. Decisión driver.js vs Onborda en [[04-Decisiones-Tecnicas/ADR-024-Vistas-Guiadas-Driver]].

**[Tours]:** Segundo y tercer flujo (mismo PR #62): dashboard + sidebar y página pública de aprobación.
- `src/lib/tours/dashboard.ts` (7 pasos: 5 secciones del sidebar + preferencias + métricas) y `src/lib/tours/approval.ts` (4 pasos, tono para el cliente).
- `src/components/tours/TourButton.tsx`: botón compartido por id de tour (client) — las páginas server lo montan sin pasar funciones; `CutPlanner` refactorizado para usarlo.
- `startOverview` ahora resuelve el primer match **visible** por selector (`checkVisibility`) — el sidebar existe 2 veces en el DOM (drawer móvil + aside desktop).
- Anclas: `NavSection` acepta prop `tour` (data-tour), footer del sidebar (`nav-prefs`), `DashboardMetrics` (`dash-metrics`), `SummaryTiles`/`ApprovalFilters`/`ApprovalDock`/tabla de `ApprovalClient` (`approval-*`).
- Tests: `tests/components/tours.test.tsx` (anti-drift dashboard + arranque) y caso nuevo en `ApprovalClient.test.tsx`. Del review del PR #62 se aplicó `side: 'bottom'` en los pasos del header de corte.

**Motivo:** los 3 flujos a enseñar al equipo son la entrada al sistema, el corte y la aprobación del cliente; los tours son opcionales por decisión explícita (no obligan a nadie).
